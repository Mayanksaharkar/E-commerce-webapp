export type LoginCredential = {
  email: string;
  password: string;
};
export type NewUser = {
  fname: string;
  lname: string;
  email: string;
  password: string;
  rePassword: string;
};
export type User = {
  name: string;
  email: string;
  mobile_no: number;
  address: string;
  pincode: number;
};
